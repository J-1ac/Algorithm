#include <bits/stdc++.h>
#include <iostream>
using namespace std;

/* how to solve
    
*/

int main(){
    int n, num, cp, cs, cm, cd, min = INT_MIN, max = INT_MAX; vector<int> an;
    cin >> n;
    for(int i=0; i<n; i++){
        cin >> num;
        an.push_back(num);   
    }
    cin >> cp >> cs >> cm >> cd;

    cout << min << "\n" << max;

    
    return 0;
}